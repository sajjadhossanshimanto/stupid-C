#include <bits/stdc++.h>
using namespace std;


int main(){
    queue<int> q;
    q.back;
    q.front;
    
    q.push;
    q.pop;

    q.empty;
    q.size;

    // extra
    q.swap;
    q.emplace;
    
    return 0;
}